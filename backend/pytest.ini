[pytest]
testpaths = tests
markers =
    integration: marks tests that require live external services (run with pytest -m integration)

# Suppress known warnings
filterwarnings =
    ignore::requests.exceptions.RequestsDependencyWarning
